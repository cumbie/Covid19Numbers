<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:prism="http://prismlibrary.com"
             xmlns:oxy="clr-namespace:OxyPlot.Xamarin.Forms;assembly=OxyPlot.Xamarin.Forms"
             prism:ViewModelLocator.AutowireViewModel="True"
             x:Class="Covid19Numbers.Views.GlobalCurvesPage"
             Title="Global Curves">

  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="auto"/>
      <RowDefinition Height="auto"/>
      <RowDefinition/>
    </Grid.RowDefinitions>

    <Label Text="Global Curve"/>
    <Picker Grid.Row="1" ItemsSource="{Binding AvailableCurves}" SelectedItem="{Binding SelectedCurve}" />

    <oxy:PlotView Grid.Row="2" Model="{Binding CurveModel, Mode=TwoWay}"
                  Margin="2,0"
                  VerticalOptions="FillAndExpand" HorizontalOptions="FillAndExpand" />

  </Grid>
  
</ContentPage>